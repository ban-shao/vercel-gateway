# Vercel Gateway 配置

# 上游 Vercel AI Gateway 地址
VERCEL_GATEWAY_URL=https://ai-gateway.vercel.sh

# 代理服务监听端口
PORT=3001

# 认证密钥（客户端调用时需要）
AUTH_KEY=your-auth-key-here

# 密钥冷却时间（小时）
COOLDOWN_HOURS=24

# 密钥文件路径
KEYS_FILE=data/keys/keys_high.txt

# 冷却密钥记录文件
COOLDOWN_FILE=data/keys/cooldown_keys.json

# 日志目录
LOG_DIR=logs

# ============== 参数处理配置 ==============

# 工作模式说明：
# 1. 透传模式（默认推荐）：直接转发请求，不做参数修改
#    - 适合 Cherry Studio 等已正确处理参数的客户端
#    - Cherry Studio 使用 Vercel AI SDK 自动处理参数格式
#    - 保持 ENABLE_PARAMS_CONVERSION=false
#
# 2. 转换模式：对请求参数进行转换
#    - 适合其他不支持 Vercel AI Gateway 参数格式的客户端
#    - 设置 ENABLE_PARAMS_CONVERSION=true

# 是否启用参数转换（默认 false，使用透传模式）
ENABLE_PARAMS_CONVERSION=false

# 是否标准化模型 ID（默认 false）
# 开启后会将简写模型名转换为完整格式
# 例如: claude-sonnet-4 -> anthropic/claude-sonnet-4-20250514
NORMALIZE_MODEL_ID=false

# 模型列表缓存时间（秒），默认 3600 秒（1小时）
MODELS_CACHE_TTL=3600
